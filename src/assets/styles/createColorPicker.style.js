import { createStyles, makeStyles } from '@mui/styles';

const useStyles = makeStyles(something => {
  return createStyles({
    chromePicker: {
      width: '80% !important',
    },
    TextValidator: {},
    AddColorButton: {},
  });
});

export default useStyles;
